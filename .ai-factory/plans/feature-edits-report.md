# План реализации: Отчёт о правках

Branch: feature/edits-report
Created: 2026-09-06

## Original Request

отчёт о правках

## Settings
- Testing: yes
- Logging: verbose
- Docs: yes

## Roadmap Linkage
Milestone: "Отчёт о правках"
Rationale: подходящей вехи в ROADMAP.md нет — «Хранение и экспорт» закрыта 2026-09-05, а отчёт вводит новый артефакт для человека рядом с файлом обмена; веху надо завести отдельно через владельца файла.

## Research Context
Source: `.ai-factory/research/edits-report/RESEARCH.md` (Active Summary, Updated: 2026-09-05 23:52, SHA256: 15f631ea3fa6d718ad5e0badf94e902c162c87ac28406e9d52235dee17f5b13f)

Topic: Отчёт о правках — печатаемый документ для человека рядом с файлом обмена

Goal: дать человеку, который передаёт задачу разработчику, читаемый документ
со своими правками и замечаниями — с картинкой места каждой правки. Файл обмена
JSON при этом остаётся главным продуктом и не меняется ни в одном байте.

Scope:
- В границах: сборка отчёта из накопленных записей, печать средствами браузера,
  снимок места правки «вырезкой» вёрстки, хранение вырезок, замена второго файла
  выгрузки отчётом.
- Вне границ: формат обмена (`format: 1`, состав `Entry`, шапка `about`),
  собственный генератор PDF, растровые скриншоты страницы, отчёт круга проверки
  (отложен до вехи «Импорт, разбор и режим проверки»).

Stakeholders:
- Рецензент — вносит правки в браузере, у него в хранилище лежат записи и вырезки.
- Менеджер — читает отчёт и передаёт задачу. ГЛАВНЫЙ читатель документа.
- Разработчик — получает файл обмена и остаток работы из отчёта.
- ИИ-агент — читает только JSON, отчёта не видит и видеть не должен.

Constraints:
- Бюджет бандла 100 КБ gzip. Замер 2026-09-05 на `dist/kalka.js`: 107 818 байт raw,
  33 695 байт gzip. В единицах самого проекта (`scripts/check-bundle.mjs` делит
  на 1024) это занято 32.9 КБ, свободно 67.1 КБ. Цифра ЗАМЕНЯЕТ более раннюю
  «занято 29 КБ, запас 71 КБ» из `.ai-factory/RESEARCH.md`. Причина расхождения
  НЕ установлена: методика прежнего замера в durable-файле не описана. Известно
  лишь, что `dist/kalka.js` датирован 22:14, то есть позже той сессии (17:32), —
  пересборка правдоподобна, но не подтверждена.
- Потолок прироста на отчёт целиком: 15 КБ gzip. Не влезло — урезается отчёт.
- Ни одного внешнего адреса в бандле: проверка `scripts/check-bundle.mjs` роняет сборку.
- Виджет — гость на чужой странице: сайт-носитель не меняется, ошибка внутри виджета
  не имеет права уронить сайт (NFR-06).
- Весь текст интерфейса и отчёта по-русски и без технических терминов (FR-36):
  читатель не видит слов «JSON», «селектор», «якорь», «DOM».
- Наружу не уходит ничего: печать и хранение локальны.
- Правила слоёв FSD (`.ai-factory/ARCHITECTURE.md`, «Правила зависимостей»):
  соседние слайсы одного слоя не импортируют друг друга; общий словарь лежит
  слоем ниже.

Requirements:
- Отчёт покрывает ВСЕ записи, а не только те, которые агент не применит:
  выбор пользователя от 2026-09-05.
- Каждая запись в отчёте показывается картинкой места правки.
- Кадр разный по типу записи: у `comment` (обведённая область, указатель) —
  широкий, с окрестностью, потому что без окрестности «это убрать» не значит ничего;
  у `text-override` и `style-wish` — узкий, по самому элементу.
- Отчёт называет свой файл обмена по имени, чтобы пара не разошлась в переписке,
  и словами говорит, что правки применяются из файла, а не из документа.
- Сквозная нумерация: номер в отчёте совпадает с номером метки на странице
  и с номером записи в списке разбора.
- Порядок разделов подчинён читателю-менеджеру: вперёд то, что требует решения
  человека (`style-wish`, `comment`), назад то, что агент применит сам (`text-override`).

Decisions:
- DEC-001. PDF получаем печатью сгенерированного документа средствами браузера,
  своего генератора PDF не пишем и библиотеки не берём. Обоснование и отвергнутые
  варианты — [ADR-0001](ADR-0001-print-instead-of-pdf-generator.md). Статус: accepted
  (выбор пользователя от 2026-09-05).
- DEC-002. Картинка места правки — «вырезка»: клон элемента с вкомпилированными
  вычисленными стилями, вклеенный в документ отчёта как вёрстка, а не как растр.
  [ADR-0002](ADR-0002-dom-cutout-instead-of-raster.md). Статус: accepted —
  гейт 2026-09-05 на живом прототипе прошёл: 3 случая из 4 совпали с эталоном,
  четвёртый провалился предсказуемым образом и был обнаружен проверкой
  контраста. Провал в замерах ровно один, так что обнаружение подтверждено
  на n=1, а порог ещё не назначен (см. RISK-001 и DEC-007).
- DEC-003. Вырезки живут в ЧЕРНОВОМ БУФЕРЕ со сроком жизни: пишутся в момент
  правки, переживают переходы между страницами разбора и стираются сразу после
  печати. Буфер лежит в `localStorage` рядом с записями — второго механизма
  хранения не появляется, и формулировка `.ai-factory/DESCRIPTION.md` («База
  данных: нет. Хранилище — `localStorage`») остаётся верной.
  При нехватке места жертвуется вырезка, а не запись. Потолок буфера теперь
  выводится из замера: 576–1260 байт gzip на вырезку (гейт 2026-09-05), то есть
  разбор из 14 записей — порядка 12 КБ gzip против ~5 МБ квоты. Буфер перестал
  быть угрозой записям; конкретное число назначается в плане.
  [ADR-0004](ADR-0004-scratch-buffer-for-cutouts.md). Статус: accepted
  (выбор пользователя от 2026-09-05).
  Отдельный склад в IndexedDB рассмотрен и отклонён:
  [ADR-0003](ADR-0003-indexeddb-for-cutouts.md), статус superseded.
- DEC-013. Запись без вырезки — старая, отбракованная самопроверкой (DEC-007)
  или не влезшая в потолок — показывается текстом с ЯВНОЙ пометкой, что место
  показать картинкой не удалось. Читатель обязан различать «картинки нет»
  и «места нет»; молчаливый пропуск выглядел бы как недоделанный отчёт,
  а исключение записи из отчёта молча потеряло бы правку заказчика.
  Тот же принцип, что у уехавших якорей и тройки исходов проверки.
  Выбор пользователя от 2026-09-05.
- DEC-014. Вид документа — ДЕЛОВАЯ ЗАПИСКА для менеджера, а не корректорский
  лист: шапка с адресом, датой, автором и числом правок; рамка с именем файла
  обмена и указанием отправить его вместе с документом; два раздела по
  трудозатратам («Требует решения» вперёд, «Правки текста» назад); сквозная
  нумерация. Метафора кальки остаётся в самом виджете; печатный документ
  отвечает на вопрос «сколько тут работы и что с этим делать».
  Выбор пользователя от 2026-09-05.
- DEC-016. Документ отчёта обязан нести правила `@font-face` сайта-носителя,
  а не только имена семейств. Гейт показал: в изолированном документе
  `document.fonts` пуст, объявленное семейство подменяется системным, и метрики
  расходятся — строка «55–200 т» заняла 58.6px на сайте и 60.66px в вырезке
  (те же 60.66, что на чистом системном шрифте). Трёх процентов хватает, чтобы
  сместить перенос строки. Для отчёта, где «было/стало» должно выглядеть как
  на сайте, подмена шрифта недопустима. Печать идёт с того же origin, поэтому
  правила `@font-face` копируются вместе с их адресами и файлы загружаются.
- DEC-015. Общая точка сборки съёмки вырезки — слой `features`. Записи типа
  `comment` создаются из `features/draw-area` и `features/place-point`,
  правки текста — из `features/edit-text`; все три лежат на одном слое,
  и только он видит оба нужных `entities`. Это тот же приём, которым уже
  собираются `anchor` и контекст для агента.
- DEC-004. Отчёт ЗАМЕНЯЕТ второй файл выгрузки — читаемую таблицу Markdown
  (`toTableText` в `src/entities/entry/model/serialize.ts`). Выгрузка становится
  «один файл + печать». Требует поправки FR-27 («два файла») и сценария А
  раздела 6 PRD, а также ДВУХ мест в `.ai-factory/DESCRIPTION.md`: «Обмен идёт
  файлом: рецензент экспортирует JSON и читаемую таблицу» и «**Экспорт двух
  файлов**: JSON для агента и импорта, читаемая таблица». Сверх того ЖЁСТКИЕ
  пункты чек-листов в навыке проекта, о которые споткнётся исполнитель:
  `.claude/skills/kalka-exchange-format/SKILL.md:270,329` («## Экспорт: два файла»,
  «- [ ] Экспорт даёт два файла…») и
  `.claude/skills/kalka-exchange-format/references/export-import.md:7,275`
  («FR-27 требует два файла», «- [ ] Экспорт даёт ровно два файла»).
  И шапка `src/features/export-entries/model/export.ts:14` — «Выгружает набор
  правок двумя файлами (FR-27)» — станет ложной, то есть правок кода по DEC-005
  получается две: вторая загрузка и этот комментарий. Выбор пользователя
  от 2026-09-05.
- DEC-005. Файл обмена не меняется: ни `format: 1`, ни состав `Entry`,
  ни шапка `about`, ни `toExchangeEntry`/`buildExchangeFile`/`toJsonText`.
  Вырезка не становится полем записи — иначе она попала бы в файл, который читает
  агент, вопреки FR-46. Правок существующего кода ровно две, обе
  в `src/features/export-entries/model/export.ts`: вторая загрузка и шапка файла,
  обещающая выгрузку двумя файлами (строка 14) — перечень в DEC-004.
- DEC-006. Вырезка снимает ИСХОДНОЕ состояние элемента, а не результат правки:
  картинка отвечает на «где это на сайте сейчас» и служит парой к строке «было».
  Точка съёмки — функция `captureDraft`
  (`src/features/edit-text/model/create.ts:29`), в шапке которой на строке 21
  сказано: «`was` и `wasHtml` захватываются ТОЛЬКО здесь, то есть только
  для новой записи». При повторной правке уже правленного места
  (FR-10) вырезка не переснимается — это желаемое поведение, а не упущение.
- DEC-007. Вырезка проверяет себя в момент съёмки, но НЕ по размерам и тексту:
  гейт 2026-09-05 показал, что худший случай проходит такую сверку насквозь —
  у провалившейся вырезки размеры и текст совпали с оригиналом идеально.
  Проверяется КОНТРАСТ цвета текста с тем фоном, который вырезка получит
  в отчёте. Замеры на живом прототипе: годные вырезки дали 5.62, 15.49 и 15.67,
  провалившаяся — 1.00 (белым по белому). Порог назначается в плане; на четырёх
  измеренных случаях разрыв между 1.00 и 5.62 велик, и любое значение внутри
  него разделяет их верно — но четыре случая выборкой не являются.
  При провале вырезка выбрасывается, запись помечается «картинку показать
  не удалось» (DEC-013). Отчёт никогда не показывает кривую картинку —
  либо верную, либо честные слова.
- DEC-008. Порядок сборки вырезки — три шага, и первые два не путать:
  собрать в контейнере шириной `viewport.w` (вёрстка встаёт как у рецензента),
  обрезать по bounding box элемента, и только потом масштабировать —
  ТОЛЬКО если после обрезки всё ещё шире колонки отчёта. Строка текста печатается
  1:1 и читается; карточка ужимается и остаётся узнаваемой.
- DEC-009. Съёмка идёт вне главного такта: клонирование с вычислением стилей
  не имеет права подтормаживать чужую страницу. Правка сохраняется мгновенно,
  вырезка догоняет. Гейт показал, что сборка занимает до 0.9 мс
  на элемент: 0.9, 0.5, 0.1 и одно значение ниже разрешения таймера страницы.
  Значит это осторожность про будущие тяжёлые случаи, а не необходимость
  на измеренных.
- DEC-010. Файл обмена скачивается первым, до всякой работы над отчётом,
  и сбой отчёта его не отменяет. Сегодня `exportEntries` возвращает `false`,
  если не встала вторая загрузка, и набор НЕ помечается экспортированным
  (`src/features/export-entries/model/export.ts`); с печатью это поведение
  неверно — JSON уже на диске.
- DEC-011. Отчёт круга проверки делается тем же рендерером, но после вехи
  «Импорт, разбор и режим проверки»: тройки исходов `ok`/`bad`/`unclear` пока
  не из чего собирать. Сегодня закладывается только необязательное поле исхода
  в модели данных отчёта — без «конфигурации режимов».
- DEC-012. Вырезки не переживают экспорт: их нет в файле обмена, и разработчик,
  импортировавший JSON, отчёт с картинками не получит. Транспорт для картинок —
  сам напечатанный PDF.

Risks:
- RISK-001 (ЗАКРЫТ гейтом 2026-09-05, остаётся остаточная часть). Вырезка врёт:
  клон, вырванный из страницы, выглядит не как оригинал. Гейт на живом
  `https://example.com/` при ширине окна 1440: три случая из четырёх
  совпали с эталоном визуально — абзац в светлой секции, карточка в сетке
  (с фотографией, четырьмя пилюлями и ссылкой) и белый заголовок на тёмной
  секции. Приём «фон ближайшего непрозрачного предка» на тёмной секции сработал.
  Провалился один случай: заголовок героя, где фон рисуют не предки,
  а СОСЕДНИЕ абсолютно позиционированные слои — `<img>` и градиентный div.
  Ближайший непрозрачный предок там белый, то есть вырезка дала белый текст
  на белом. Остаточный риск: этот класс вёрстки распространён (герои, баннеры,
  наложения), и ПОЧИНКА его не проверялась — проверялось только обнаружение.
  Оно сработало на единственном наблюдавшемся провале (DEC-007); что оно ловит
  этот класс вообще, а не один его экземпляр, — вывод, а не замер. Выбор «все записи» умножает число картинок,
  но не долю провальных.
- RISK-002. Менеджер отправляет разработчику ТОЛЬКО PDF, потому что он читаемый
  и выглядит достаточным. Агенту тогда нечего применять, и возвращается ровно
  та боль, ради которой затеян продукт (PRD, раздел 1 «Проблема», строка 40:
  заказчик «делает скриншот, вставляет его в мессенджер и пишет „вот этот текст
  поправь на такой“»). Смягчение — отчёт называет свой файл обмена
  и говорит словами, что правки применяются из него.
- RISK-003 (сильно уменьшен замером). Размер вырезки не ограничен: обведённая
  область может накрыть секцию в сотни узлов. Но замер на живом прототипе дал
  порядок много меньше ожидаемого: 576–1260 байт gzip на вырезку (абзац 708,
  карточка из 11 узлов с картинкой 1260, заголовок 576, заголовок героя 658).
  Это в ~25 раз меньше расчётных «~30 КБ», на которых строился спор о хранилище.
  Потолок всё ещё нужен — секция в сотни узлов не измерялась, — но угроза
  переполнения буфера из штатного режима ушла.
- RISK-004. Печать без фоновой графики: браузер по умолчанию не печатает фоны,
  и вырезка приедет белым листом. Смягчение — `print-color-adjust: exact`,
  проверить во всех четырёх браузерах.
- RISK-005. Печать из `<iframe>`, лежащего внутри теневого корня; Safari
  исторически требовал ненулевой видимости узла. Отступной путь — отдать отчёт
  файлом на скачивание, рендерер тот же.
- RISK-006. Картинки внутри вырезки грузятся с сервера прототипа по абсолютным
  адресам. Работает, пока печатают с того же origin. Запрет внешних адресов
  из `check-bundle.mjs` относится к БАНДЛУ, а не к содержимому отчёта —
  конфликта нет, но формулировку легко перепутать.

Open questions:
- Как ПОЧИНИТЬ случай героя — фон из соседних абсолютных слоёв, — не проверялось.
  Разведка показала, что под заголовком лежит градиентный `<div>` во всех пяти
  пробных точках, то есть материал для фона доступен; но собрать из него фон
  вырезки не пробовали. Пока такие места деградируют в текст (DEC-013).
- Порог контраста для отбраковки (DEC-007) не назначен. Замеры дают разрыв
  между 1.00 и 5.62 — любое значение внутри разделяет верно, но выбирать
  число надо на большем числе примеров, чем четыре.
- Конкретный потолок буфера и порядок вытеснения назначаются в плане: порядок
  величины известен (12 КБ gzip на разбор из 14 записей), само число — нет.
- Момент очистки буфера завязан на успех печати, а печать средствами браузера
  о своём исходе не сообщает (ADR-0001). Чем именно считать «успешной печатью»,
  не решено.
- Внутри выбранного вида документа (DEC-014) не проработаны разворот записи,
  колонтитулы и разрывы страниц — это работа вёрстки, а не выбор.
- Типичный состав разбора не измерялся. Число «14 правок, из них 10 текстовых»
  используется в расчётах объёма и длины документа как иллюстрация, а не как
  установленный факт. Замер стоит снять на первом же круге с рецензентом.
- Причина расхождения с прежним замером бюджета («29 КБ» против нынешних
  32.9 КБ) не установлена — методика прежнего замера не записана.

Success signals:
- Менеджер, получивший отчёт, отправляет разработчику ОБА файла, а не один.
- По номеру в отчёте разработчик находит место на странице и обратно.
- Отчёт не разрастается: на разборе из 14 правок — 4–5 страниц, а не 15.
  Число 14 взято для расчёта и типичным не подтверждено.
- Ни одна кривая вырезка не попала в документ: самопроверка отсекает их до печати.
- Файл обмена не изменился: выгруженные ранее файлы остаются валидными,
  агент читает их тем же способом.
- Бандл остаётся в пределах 100 КБ gzip, прирост отчёта не выходит за 15 КБ.
- Ни одна вырезка с контрастом ниже порога не попала в напечатанный документ.

Next step: `/aif-plan full отчёт о правках`. Гейт RISK-001 пройден 2026-09-05
на живом `https://example.com/`: три случая из четырёх совпали,
четвёртый проваливается предсказуемо и обнаруживается проверкой контраста.
Растровый путь (snapdom, 52.0 КБ gzip) к обсуждению не возвращается.

## Уточнения плана к исследованию

Три места, где реализация отходит от буквы исследования. Первые два — уточнения, третье — пересмотр решения; если не согласны, правится до начала работ.

**1. Момент очистки буфера (уточняет DEC-003).** Исследование говорит «стираются сразу после печати», и это предложение пользователя. Буквальная реализация ставит ловушку: печать средствами браузера о своём исходе не сообщает, значит успехом пришлось бы считать открытие диалога, — а человек, отменивший диалог, чтобы что-то проверить, при повторной печати получит отчёт уже без картинок. Замер снял срочность очистки: разбор весит порядка 12 КБ, а не 420. Поэтому буфер очищается **при первом изменении набора правок после печати**, а не в момент постановки печати. Отчёт можно перепечатать сколько угодно раз, пока рецензент не вернулся к правке; смысл «после формирования отчёта вырезки не нужны» сохраняется.

**2. Функция `styleWords` не удаляется вместе с читаемой таблицей (уточняет DEC-004).** Пожелание оформления словами («размер 28px, цвет тёмный») нужно отчёту ровно так же, как было нужно таблице. Функция остаётся в `entities/entry` и выносится в публичный API слайса — **задачей 8, а не задачей 11**: без неё не собирается сам отчёт, а задача 11 идёт после отчёта, и вынос на неё дал бы цикл 8↔11.

**3. Вырезка снимается и в момент отчёта, а не только в момент правки (пересматривает DEC-006).**
Исследование требует, чтобы на картинке было ИСХОДНОЕ состояние места. Из этого
не следует, что снимать надо в момент правки, — а первая редакция плана прочитала
именно так, и съёмка в момент правки стала единственным путём. Следствие оказалось
тяжёлым: правки, сделанные до появления вырезок, остались без картинок навсегда,
буфер из запаса стал обязательным звеном, а отчёт из шести записей без единой
картинки читается как сломанный.

Код показывает, что запрет был лишним. Записи типов `comment` и `style-wish`
в DOM не применяются НИКОГДА (`src/app/lib/overlay/engine.ts:320`) — их элемент
нетронут, и вырезка, снятая позже, побайтово та же. У `text-override` элемент несёт
`now`, но оригинал лежит в `wasHtml`, и код его восстановления уже написан и работает:
`engine.ts:169`, `el.replaceChildren(sanitizeHtml(entry.wasHtml, root))` — на этом
держится тумблер «показать оригинал». Найденный элемент тоже уже есть: движок кладёт
его в `EntryState.element`, и `entryStore.statusOf(id)` его отдаёт.

Поэтому **этот пункт заменяет рассуждение из задачи 6** («снимать на сохранении
нельзя»): узко оно верно, но читается как запрет снимать позже вообще. Съёмка
в момент правки остаётся — она переживает переход между страницами; съёмка в момент
отчёта добавляется как второй источник для записей, чьё место найдено на текущей
странице.

Две честные оговорки, которые надо держать в виду при реализации:
— подставленный `wasHtml` теряет вкомпилированные стили вложенных узлов.
  Унаследованные с корня шрифт, размер, цвет и межстрочный интервал остаются,
  а разметка внутри (`<b>`, `<i>`, ссылка, список) отрисуется собственным видом
  браузера. Для строки текста это обычно незаметно, но пиксель-в-пиксель
  не обещается;
— размер кадра измеряется по ЖИВОМУ элементу, то есть по тексту «стало».
  Если «было» заметно длиннее, край вырезки будет обрезан. Померить клон нельзя:
  он отсоединён от документа.

**4. Кадр берётся во всю ширину страницы полосой (пересматривает DEC-008 и DEC-006).**
Исследование делило кадры на узкие (правки текста, «строка печатается 1:1») и широкие
(замечания, «с окрестностью»). Печать 2026-09-06 показала, что деление неверно в самой
своей посылке. Узкий кадр отвечает на вопрос «что исправить» — но на этот вопрос уже
отвечают строки «Было/Стало» под картинкой, причём точнее и полным кеглем. На вопрос
«ГДЕ это на странице» узкий кадр не отвечает вовсе: строка, вырванная из вёрстки,
узнаётся ничуть не лучше, чем тот же текст. Выбор пользователя от 2026-09-06.

Поэтому кадр теперь один для всех типов: **полоса во всю ширину страницы, с запасом
сверху и снизу**. Разделение труда стало явным — картинка отвечает «где», строки
рядом отвечают «что».

Три следствия, названные честно:
— **прощаемся с «1:1»**. Кадр в ширину окна (1512) ужимается в колонку отчёта (652),
  масштаб 0.43, и текст на картинке мелкий. Это плата за узнаваемость места,
  и она осознанная: читать правку по картинке больше не нужно;
— **ширина вырезки всегда равна ширине страницы, даже когда найденный предок уже её**. Блок,
  лежащий прямо в `<body>` без секции вокруг, полной ширины не достигает — его
  единственный полноширинный предок это сам `<body>`, а он тащит шапку и подвал.
  Замерено на стенде: секция дала масштаб 0.43, а блок 0.73, и соседние картинки
  одного документа разъехались в размере без всякой причины в вёрстке. Решено
  холстом: вырезка это холст шириной со страницу, внутри которого найденный элемент
  стоит на своём месте, а поля по краям крашены фоном из-под него;
— **дефект 5 закрылся сам собой.** Прежде кадр правки текста показывал одну строку
  из трёх: высота бралась по живому элементу с коротким «стало». Полоса с запасом
  в 80 пикселей покрывает подставленное «было» целиком — проверено печатью.
  Оговорка в шапке `build.ts` остаётся: правка, выросшая НАМНОГО, всё ещё выйдет
  за полосу, и закрыть это до конца может только замер клона за экраном.

## Commit Plan

- **Commit 1** (задачи 1–2): `chore(cutout): тип вырезки, константы и чистая геометрия`
- **Commit 2** (задачи 3–6): `feat(cutout): снимок места правки, самопроверка и черновой буфер`
- **Commit 3** (задачи 7–9): `feat(report): печатаемый документ и метки поверх вырезки`
- **Commit 4** (задачи 10–12): `feat(report): кнопка «Отчёт», выгрузка одним файлом, очистка буфера`
- **Commit 5** (задачи 13–14): `test(report): покрытие` и `docs: синхронизация PRD, описания, README и навыка`
- **Commit 6** (задачи 15–16): `test(report): стенд печати, прогон по браузерам и замер бандла`
- **Commit 7** (задачи 17–19): `feat(report): вырезка снимается и в момент отчёта`
- **Commit 8** (задача 20 и остаток задачи 15): `test(report): шрифт на стенде и прогон печати по браузерам`
- **Commit 9** (задача 21): `feat(cutout): кадр во всю ширину страницы полосой вокруг правки`

## Tasks

### Фаза 1. Основание: типы, константы, геометрия

- [x] **Задача 1: Тип вырезки и константы.**
  Объявить `Cutout` в `src/shared/model/layer.ts` — **не** в `format.ts`. Вырезка не попадает в файл обмена (DEC-005, DEC-012), а `layer.ts` для того и заведён: «Понятия слоя правок, существующие только в рантайме виджета. В файл обмена они не попадают». Поля: `html` (разметка со вкомпилированными стилями), `width`/`height` (размер кадра в px на момент съёмки), `anchorBox` (положение и размер элемента-якоря ВНУТРИ кадра, в px кадра: `{ x, y, w, h }`), `fontFaces` (правила `@font-face` носителя, DEC-016), `at` (ISO 8601).
  **Зачем `anchorBox` отдельным полем.** `captureRect`/`capturePoint` (`src/shared/lib/geometry.ts:49,67`) хранят доли от bounding box ЭЛЕМЕНТА-ЯКОРЯ, а кадр записи `comment` — широкий, с окрестностью (задача 6), то есть кадр большего элемента. Подставить доли прямо в `width`/`height` значит промахнуться меткой, причём ровно у того типа записи, весь смысл которого «вот это здесь». Совпадение кадра с якорем — частный случай (`anchorBox` равен всему кадру у узких кадров правок текста), а не правило.
  В `src/shared/config/constants.ts` добавить с развёрнутым JSDoc в стиле файла: `CUTOUTS_KEY = 'cutouts'` (ключ буфера рядом с `ENTRIES_KEY`), `CUTOUT_MAX_BYTES` (потолок одной вырезки; замер дал 1.2–11.7 КБ разметки на элемент, но обведённая область в сотни узлов не измерялась — назвать выбранное число и его причину), `CUTOUT_BUFFER_MAX_BYTES` (потолок всего буфера), `CUTOUT_MIN_CONTRAST` (порог самопроверки; замеры: годные 5.62/15.49/15.67, провальная 1.00), `CUTOUT_CAPTURE_IDLE_MS` (отсрочка съёмки вне главного такта).
  Файлы: `src/shared/model/layer.ts`, `src/shared/config/constants.ts`.
  **Логирование:** рантайма в задаче нет — объявления типов и константы ничего не исполняют, логировать нечего. Не добавлять логгер «на будущее».

- [x] **Задача 2: Чистая геометрия для отрисовки метки в отчёте.**
  В `src/shared/lib/geometry.ts` добавить `rectFromBox(rect: RectAnchor, box: { w: number; h: number })` и `pointFromBox(point: PointAnchor, box: { w: number; h: number })` — чистые функции «доли → пиксели относительно кадра известного размера», без обращения к `getBoundingClientRect`. Существующие `restoreRect`/`restorePoint` переписать через них, **не меняя сигнатур и поведения**: сейчас они жёстко завязаны на живой элемент в текущем документе, а отчёту нужен тот же расчёт для вырезки, которой в DOM нет.
  Файлы: `src/shared/lib/geometry.ts`, `src/shared/lib/geometry.test.ts`.
  **Логирование:** не добавлять. `geometry.ts` — чистая арифметика без логгера, и это осознанно: функция вызывается на каждом кадре прокрутки.

### Фаза 2. Вырезка: съёмка, самопроверка, буфер

- [x] **Задача 3: Сборка вырезки.** (зависит от 1)
  Новый слайс `src/entities/cutout/` с `index.ts` и `model/build.ts`. Функция `buildCutout(el: Element, viewportWidth: number): Cutout | null`:
  1. глубокий клон элемента; в клон вкомпилировать вычисленные стили **из закрытого списка свойств** (перечислить список в коде с комментарием, почему именно эти) — значения `getComputedStyle` уже разрешены, поэтому тема через `:root` и каскадные переменные переносятся верно;
  2. собрать вёрстку в контейнере шириной `viewportWidth`, затем обрезать по bounding box элемента, и только потом масштабировать, если кадр шире колонки отчёта — **три шага, первые два не сливать** (DEC-008: ширина контейнера нужна для правильной вёрстки, а не для кадра). На шагах обрезки и масштабирования заполнить `anchorBox` — положение и размер элемента-якоря в координатах готового кадра: у широкого кадра `comment` якорь занимает лишь часть кадра, и без этого поля метка задачи 9 встанет мимо;
  3. собрать правила `@font-face` документа-носителя (DEC-016) — без них семейство молча подменяется системным и уезжают переносы строк. **Каждая таблица стилей читается отдельно и под защитой** (`safely` из `shared/lib/safe` либо try): обращение к `sheet.cssRules` у таблицы с другого origin бросает `SecurityError`, а шрифты как раз обычно и раздаются с CDN — то есть отказ приходится ровно на тот случай, ради которого DEC-016 и написан. Довод DEC-016 «печать идёт с того же origin» говорит про origin документа отчёта, а не про origin таблицы стилей. Недоступные таблицы пропускать с WARN и продолжать сбор; пустой набор правил съёмку не отменяет — шрифт подменится системным, и это честная деградация, а не отказ. Исключение наружу не уходит (NFR-06);
  4. адреса картинок привести к абсолютным;
  5. вырезать `script`, `style`, `noscript`; снять атрибуты `class` (стили уже инлайновые); снять **все атрибуты, начинающиеся на `on`**, и `href`/`src` со схемой вне `http:`/`https:`/`mailto:`. Документ отчёта уезжает в `<iframe srcdoc>`, а такой iframe **того же origin**, что страница-носитель: уцелевший `onerror` у `<img>` или `onclick` у ссылки исполнится в origin прототипа прямо в печатном документе, и изоляции теневого корня там уже нет. Приём в проекте отработан — белый список схем и поштучное снятие атрибутов в `src/entities/entry/model/sanitize.ts:36,83-89`, там же довод «схемы вроде `javascript:` и `data:` до элемента не доходят»; сослаться на него комментарием. Импортировать `sanitizeHtml` нельзя: `entities/cutout` и `entities/entry` — соседние слайсы одного слоя, поэтому чистка своя;
  6. при превышении `CUTOUT_MAX_BYTES` вернуть `null` — деградация честная, отчёт покажет текст с пометкой (DEC-013).
  Файлы: `src/entities/cutout/index.ts`, `src/entities/cutout/model/build.ts`.
  **Логирование:** `createLogger('cutout:build')`. DEBUG на входе — тег, размеры кадра, число узлов; DEBUG на выходе — размер разметки в байтах и затраченные миллисекунды; WARN при превышении потолка с указанием размера и потолка. **В лог не уходят ни текст элемента, ни разметка, ни адрес сайта** — это содержимое чужой страницы (правило `shared/api/storage.ts` и `files.ts`).

- [x] **Задача 4: Самопроверка вырезки по контрасту.** (зависит от 1)
  `src/entities/cutout/model/check.ts`. Функция `checkCutout(el: Element): { ok: boolean; contrast: number }`:
  — найти фон ближайшего непрозрачного предка (этот приём разбирает тёмные секции верно);
  — вычислить относительную яркость и контраст по формуле WCAG между цветом текста элемента и найденным фоном;
  — сравнить с `CUTOUT_MIN_CONTRAST`.
  **Проверяется ЭЛЕМЕНТ-ЯКОРЬ, а не кадр.** У записи `comment` кадр широкий, с окрестностью (задача 6), то есть кадр большего элемента, чем якорь. В `checkCutout` приходит именно якорь — тот элемент, к которому привязана запись и чей текст рецензент и имел в виду. Проверять контейнер широкого кадра бессмысленно: его собственный цвет текста к правке отношения не имеет. Ширина кадра на выбор проверяемого элемента не влияет никогда.
  **Почему не сверка размеров и текста:** гейт 2026-09-05 показал, что провалившаяся вырезка совпала с оригиналом и по размерам, и по тексту, то есть прошла бы такую проверку насквозь (DEC-007). Контраст поймал её на значении 1.00 против 5.62–15.67 у годных.
  Ограничение записать комментарием: обнаружение подтверждено на **одном** наблюдавшемся провале; что оно ловит класс «фон рисуют соседние слои» вообще — вывод, а не замер.
  Файлы: `src/entities/cutout/model/check.ts`, `src/entities/cutout/index.ts`.
  **Логирование:** `createLogger('cutout:check')`. DEBUG на каждую проверку — вычисленный контраст, цвет текста, найденный цвет фона, вердикт; WARN при отбраковке с теми же полями. Цвета логировать можно: это оформление чужой страницы, а не содержимое правок.

- [x] **Задача 5: Черновой буфер вырезок.** (зависит от 1)
  `src/entities/cutout/model/buffer.ts` поверх `src/shared/api/storage.ts` (ключ `CUTOUTS_KEY`). API: `put(entryId, cutout)`, `get(entryId)`, `has(entryId)`, `drop(entryId)`, `clear()`.
  **Несущий инвариант (DEC-003, требование «JSON главное»):** при нехватке места жертвуется вырезка, а не запись. Буфер обязан:
  — писать **только** под своим ключом и никогда не трогать `ENTRIES_KEY`;
  — при отказе `write` не выставлять флаг `persistFailed` записей (это флаг другого хранилища) и вытеснять самые старые вырезки, пока не уложится в `CUTOUT_BUFFER_MAX_BYTES`;
  — считать потерю буфера штатной: нет вырезки — отчёт деградирует в текст.
  **Второй инвариант — вырезка без записи есть мусор.** Съёмка идёт в точке создания ЧЕРНОВИКА, а черновик записью становится не всегда (см. задачу 12), поэтому в буфере копятся вырезки под идентификаторами, которых в наборе нет. Вытеснение обязано выбрасывать их ПЕРВЫМИ, до всякого вытеснения по возрасту: иначе буфер выбросит настоящую вырезку ради мусора. Сам сбор сирот — задача 12; здесь только порядок вытеснения. Для этого добавить `keep(ids: readonly string[])` — оставить в буфере только перечисленные идентификаторы, вернуть число снятых.
  Файлы: `src/entities/cutout/model/buffer.ts`, `src/entities/cutout/index.ts`.
  **Логирование:** `createLogger('cutout:buffer')`. DEBUG на `put`/`get`/`drop` — идентификатор записи, размер в байтах, текущий объём буфера; INFO на `clear` с числом снятых вырезок; WARN при вытеснении (сколько и почему) и при отказе записи. Разметку вырезки в лог не выводить.

- [x] **Задача 6: Съёмка в трёх точках создания записи.** (зависит от 3, 4, 5)
  Вырезка снимается там же, где элемент ещё не тронут слоем, и **до** наложения (DEC-006). Три точки, все на слое `features` — тот же приём, которым уже собираются якорь и контекст для агента (DEC-015):
  — `src/features/edit-text/model/create.ts`, функция `captureDraft` (строка 29; в её шапке на строке 21 записано «`was` и `wasHtml` захватываются ТОЛЬКО здесь, то есть только для новой записи») — узкий кадр по самому элементу;
  — `src/features/draw-area/model/tool.ts` и `src/features/place-point/model/tool.ts` — широкий кадр с окрестностью: без неё «это убрать» не опознать.
  Съёмка **отложенная**, вне главного такта (DEC-009): правка сохраняется мгновенно, вырезка догоняет. Замер даёт 0.1–0.9 мс, так что это осторожность про будущие тяжёлые случаи, а не необходимость.
  При повторной правке уже правленого места (FR-10) вырезка **не переснимается** — сохранённая показывает исходное состояние, что и требуется.
  **Съёмка на черновик, а не на запись, и это осознанно.** `captureDraft` зовётся на КАЖДЫЙ клик по элементу при включённом инструменте (`src/features/edit-text/model/tool.ts:76`), а записи может не появиться вовсе: `saveEdit` при сведении правки к оригиналу выходит без `upsert` (`src/features/edit-text/model/save.ts:63-75`), плюс человек просто закрывает окно. Снимать «на сохранении» нельзя — движок наложения к тому моменту уже переписал элемент, и снимок покажет не исходное состояние (DEC-006). Значит вырезки-сироты неизбежны по устройству, и убирает их сверка с набором (задача 12), а не отказ от этой точки съёмки.
  Файлы: `src/features/edit-text/model/create.ts`, `src/features/draw-area/model/tool.ts`, `src/features/place-point/model/tool.ts`.
  **Логирование:** в каждой точке — INFO «вырезка снята» с идентификатором записи, исходом самопроверки и размером; WARN при провале самопроверки или превышении потолка с причиной. Ни текста правки, ни содержимого вырезки.

### Фаза 3. Документ отчёта и печать

- [x] **Задача 7: Внешняя граница печати.**
  Новый файл `src/shared/api/print.ts` рядом с `files.ts` и по его образцу. Функция `printDocument({ html, parent }): boolean`:
  — создать `<iframe>` с `srcdoc` в `parent.ownerDocument` и добавить **в переданный `parent`** — узел теневого корня «Кальки», а не в `document.body`. Причины дословно те же, что в шапке `files.ts`: обещание «страница после демонтажа неотличима от исходной» и нежелание будить собственный наблюдатель мутаций;
  — дождаться `onload` перед вызовом `contentWindow.print()` — печать до загрузки `srcdoc` даёт пустой лист;
  — iframe не делать `display: none`: Safari исторически требовал ненулевой видимости. Нулевой размер и вынос за экран;
  — снять узел после печати, ничего не бросая наружу (NFR-06).
  Печатать **свой документ, а не страницу-носитель**: `window.print()` напечатал бы прототип с его правилами `@media print`, причём метки уехали бы — печатная вёрстка пересчитывается на бумажную ширину, а слой позиционируется нашим кодом на экране и при печати не пересчитывается.
  Файлы: `src/shared/api/print.ts`.
  **Логирование:** `createLogger('print')`. DEBUG на постановку печати — размер документа в байтах; WARN на любой сбой с текстом ошибки. **Содержимое документа не логировать никогда** — это текст правок заказчика.

- [x] **Задача 8: Сборка документа отчёта.** (зависит от 2, 5)
  Новый слайс `src/features/print-report/` с `index.ts` и `model/report.ts`. Функция `buildReport(input): string` возвращает самодостаточный HTML.
  `author` и имя файла обмена приходят **параметрами**: они живут в `features/export-entries` — соседнем слайсе того же слоя, импорт которого запрещён. Собирает их слой выше (задача 10), ровно как `features/edit-text` передаёт готовый якорь в `entities/entry`.
  Вид — **деловая записка для менеджера** (DEC-014), не корректорский лист:
  — шапка: адрес сайта, дата, автор, число правок;
  — сразу под шапкой рамка: правки текста применяются из файла `<имя файла обмена>`, отправьте его вместе с этим документом. Это единственное смягчение RISK-002 — менеджер отправляет разработчику один читаемый PDF, и агенту нечего применять. Имя приходит параметром и **может отсутствовать**: набор ещё не выгружали. Тогда имени не выдумывать — рамка говорит прямо, что файл ещё не выгружен и его надо выгрузить кнопкой «Экспорт». Названное имя несуществующего файла хуже отсутствия имени: оно выглядит достоверно и отправляет менеджера искать то, чего нет;
  — раздел «Требует решения» **впереди**: `style-wish` и `comment`, то есть то, что агент не применит и что и есть задача человеку;
  — раздел «Правки текста» после него: `text-override`, «было/стало»;
  — **правка текста с пожеланием по оформлению не теряет вторую половину.** `resolveType` (`src/features/edit-text/model/save.ts:47-53`) возвращает `text-override`, как только изменился текст, а `saveEdit` кладёт `style` в запись независимо от типа (строки 77–84): запись `text-override` с непустым `style` — штатное достижимое состояние, рецензент и текст поправил, и попросил сделать крупнее. Целиком отправить её в «Правки текста» значит спрятать пожелание в раздел, про который документ говорит «агент применит сам», — а механически агент его не применяет, ради чего тип `style-wish` и заведён (FR-39). Правило: запись остаётся в «Правках текста» под своим единственным сквозным номером (второго номера у неё быть не может), но пожелание оформления рисуется у неё отдельной **помеченной** строкой через `styleWords` — «требует решения человека», — и такие записи учитываются в счётчике «требует решения» в шапке. Показать одно «было/стало» и промолчать нельзя;
  — сквозная нумерация через `numbering()` из `entities/entry` — тот же номер, что на метке и в списке разбора; заново не пересчитывать;
  — группировка по страницам с **устойчивым** порядком: страницы идут по первому появлению записи в наборе (`entryStore.list()` уже отдаёт записи в порядке добавления), текущий маршрут в сборку документа не передаётся вовсе и на порядок не влияет. `groupByRoute` из `widgets/entry-list` (`EntryList.tsx:79`) образцом здесь служит только по форме: она вторым аргументом принимает текущий маршрут и поднимает его группу наверх, а для печати это неверно — «текущая» страница есть та, где рецензент случайно оказался в момент нажатия «Отчёт», и два прогона печати по одному набору дали бы документы с разным порядком разделов, после чего сослаться на «третью страницу отчёта» в переписке уже нельзя. Скопировать её дословно всё равно невозможно: `features/print-report` лежит ниже `widgets` и импортировать оттуда не может;
  — пожелание оформления — словами, через `styleWords` (см. «Уточнения плана», пункт 2). **Вынос `styleWords` в публичный API `entities/entry` делается ЗДЕСЬ, а не в задаче 11:** сегодня это приватная функция `src/entities/entry/model/serialize.ts:223`, без неё задача 8 не собирается, а вешать вынос на задачу 11 значило бы завести цикл 8↔11 (задача 11 удаляет таблицу и потому идёт после отчёта). Файл правки: `src/entities/entry/index.ts`;
  — запись без вырезки — текст с **явной пометкой**, что показать место картинкой не удалось (DEC-013): читатель обязан различать «картинки нет» и «места нет»;
  — печатные стили внутри самого документа: `print-color-adjust: exact` (иначе браузер не печатает фоны и вырезка приедет белым листом), `@page`, разрывы страниц, правила `@font-face` из вырезок.
  **FR-36:** в документе нет слов «JSON», «формат», «селектор», «якорь», «DOM». Имя файла показать можно — это имя, а не термин.
  Файлы: `src/features/print-report/index.ts`, `src/features/print-report/model/report.ts`, `src/entities/entry/index.ts`.
  **Логирование:** `createLogger('report')`. DEBUG на входе — число записей по типам, сколько с вырезкой и сколько без; DEBUG на выходе — размер документа в знаках; INFO «отчёт собран» с теми же количествами. **Ни текстов правок, ни адреса сайта, ни имени автора** — по образцу `export.ts`, где в лог уходят только количества.

- [x] **Задача 9: Метка поверх вырезки.** (зависит от 2, 8)
  В `src/features/print-report/model/report.ts` (или соседнем `model/mark.ts`) рисовать поверх вырезки уголки области и выноску указателя по долям — через `rectFromBox`/`pointFromBox` из задачи 2. Доли берутся из `entry.rect`/`entry.point`, а бокс для пересчёта — **`Cutout.anchorBox`, а НЕ `Cutout.width`/`Cutout.height`**, и результат сдвигается на `anchorBox.x`/`anchorBox.y` внутри кадра. Причина: `captureRect`/`capturePoint` (`src/shared/lib/geometry.ts:49,67`) хранят доли от bounding box элемента-якоря, а кадр записи `comment` — широкий, с окрестностью (задача 6), то есть кадр другого, большего элемента. Взять весь кадр значит промахнуться меткой ровно у того типа записи, весь смысл которого «вот это здесь». У узких кадров правок текста `anchorBox` совпадает со всем кадром, и формула та же. Так метка встаёт туда же, куда `MarkLayer` ставит её на живой странице, при любом масштабе.
  Номер метки на вырезке обязан совпадать с номером записи в списке — это единственная ниточка между бумагой и экраном.
  Файлы: `src/features/print-report/model/report.ts` либо `src/features/print-report/model/mark.ts`.
  **Логирование:** DEBUG на каждую нарисованную метку — идентификатор записи, тип геометрии, вычисленные пиксели. Текст комментария не логировать.

### Фаза 4. Интеграция и выгрузка одним файлом

- [x] **Задача 10: Кнопка «Отчёт» и запуск печати.** (зависит от 7, 8)
  — `src/widgets/export-preview/ui/ExportSummary.tsx`: кнопка «Отчёт» рядом с «Экспорт», неактивна при нуле правок — по образцу существующей;
  — **иконки у кнопки нет, и `IconReport` не заводится.** Соседняя «Экспорт» — текстовая кнопка без иконки (`<Button onClick={…}>Экспорт</Button>`), тип `Button` иконку у текстовой кнопки допускает, но не требует. Иконка на одной кнопке из двух соседних — это либо рябь в интерфейсе, либо необъявленная правка второй кнопки; а шапка `src/shared/ui/icons.tsx` прямо назначает цену набора («Четырнадцать иконок стоят 0,7 КБ gzip») и требует, чтобы всё растящее бандл себя называло (NFR-01). Пятнадцатая иконка ради одной текстовой кнопки этой цены не стоит. Следствие: `icons.tsx` задача не трогает вовсе, и число в его шапке остаётся верным;
  — `src/app/ui/Root.tsx`: состояние по образцу `exportOpen`/`openExport`/`closeExport`; собрать `author` (через `readAuthor` из `features/export-entries`) и имя файла обмена, передать в `buildReport`, результат отдать в `printDocument` с `parent` = корневой узел панели (тот же приём, что у `ExportPreview` с `root.current`);
  — **имя файла обмена берётся из момента реальной выгрузки, а не вычисляется заново.** `exportFileName('json', now, location)` собирает `kalka-<хост>-<ГГГГ-ММ-ДД-ЧЧММ>.json` из переданного момента, и отчёт печатают не в ту же минуту, что жали «Экспорт», — документ назвал бы файл, которого на диске нет. Момент выгрузки уже хранится и переживает перезагрузку (`entryStore.exportedAt()`, снимок — `src/entities/entry/model/persist.ts:238`), поэтому имя восстанавливается однозначно: `exportFileName('json', new Date(entryStore.exportedAt()), location)`. При `exportedAt === null` имя в `buildReport` НЕ передаётся вовсе — набор ещё не выгружали, и отчёт скажет об этом словами (задача 8). Это единственное смягчение RISK-002, и оно обязано быть точным;
  — `src/app/ui/host.css`: стили рядом с блоком `.kalka-preview*` (строки 1067–1156) — он прямой образец. Переменные только с префиксом `--kalka-`.
  Файлы: `src/widgets/export-preview/ui/ExportSummary.tsx`, `src/app/ui/Root.tsx`, `src/app/ui/host.css`.
  **Логирование:** INFO при нажатии «Отчёт» — число правок, попавших в документ; WARN если печать не встала. Логгер брать через `createLogger` со своей областью, `console` в `src/` запрещён линтером везде, кроме `shared/lib/log.ts`.

- [x] **Задача 11: Выгрузка становится одним файлом.** (зависит от 8)
  — `src/features/export-entries/model/export.ts`: убрать вторую загрузку, `TABLE_MIME` и отложенный `setTimeout`. **Пересмотреть возвращаемое значение и `markExported`:** сейчас набор не помечается экспортированным, если не встала вторая загрузка, — с одним файлом это условие исчезает, и говорить «не выгружено», когда файл на диске, значит соврать ровно в том месте, ради которого сделана FR-25 (DEC-010). Файл обмена скачивается первым и остаётся главной дорожкой: ничто из отчёта не имеет права его отменить;
  — там же поправить шапку файла: она обещает «Выгружает набор правок двумя файлами (FR-27)» и станет ложной;
  — `src/entities/entry/model/serialize.ts`: удалить `toTableText`, `rowOf`, `cell`, `formatMoment` и алиас `plainText`. **`styleWords` не удалять** — отчёту она нужна (см. «Уточнения плана», пункт 2); в публичный API слайса её выносит задача 8, здесь только не трогать. Из `src/entities/entry/index.ts` убрать `toTableText`. Проверить, что `plainHtml` из `model/verify.ts` ещё используется режимом проверки;
  — `src/features/export-entries/model/name.ts`: сузить тип параметра `ext` до `'json'`;
  — `src/features/export-entries/index.ts`: **вынести `exportFileName` в публичный API слайса.** Задаче 10 имя файла обмена нужно в `Root.tsx`, а `no-restricted-imports` в `eslint.config.js` глушит обращение внутрь слайса в обход `index.ts` — без этой строки `npm run lint` падает. Та же поправка, что у `styleWords`, только для другого слайса.
  Файлы: `src/features/export-entries/model/export.ts`, `src/features/export-entries/model/name.ts`, `src/features/export-entries/index.ts`, `src/entities/entry/model/serialize.ts`, `src/entities/entry/index.ts`.
  **Логирование:** сохранить нынешнюю дисциплину — в лог уходят только количества и признаки, ни адреса сайта, ни имени автора, ни текстов правок. Имя файла — уровнем DEBUG (содержит хост прототипа и вырезается из продакшен-сборки).

- [x] **Задача 12: Очистка буфера.** (зависит от 5, 10)
  Буфер очищается **при первом изменении набора правок после печати**, а не в момент постановки печати. Обоснование и отличие от буквы DEC-003 — в разделе «Уточнения плана», пункт 1; коротко: печать о своём исходе не сообщает, и очистка по факту открытия диалога отняла бы картинки у всякого, кто диалог отменил.
  Дополнительно: `drop(entryId)` при удалении записи (FR-11, FR-16) — вырезка без записи бессмысленна.
  **Сбор вырезок-сирот — обязательная часть задачи, а не украшение.** Съёмка идёт в точке создания черновика, а черновик записью становится не всегда: `captureDraft` зовётся на каждый клик по элементу при включённом инструменте «Текст» (`src/features/edit-text/model/tool.ts:76`), а `saveEdit` при сведении правки к оригиналу выходит без `upsert` (`src/features/edit-text/model/save.ts:63-75`); плюс человек просто закрывает окно. Значит каждый разведочный клик кладёт в хранилище вырезку под идентификатором, который записью не станет никогда. Снимать «на сохранении» нельзя (DEC-006, разбор — в задаче 6), поэтому сироты убираются сверкой: вызывать `keep(entryStore.list().map((e) => e.id))` (задача 5) **при постановке печати и при первом изменении набора после печати**. Без этого вытеснение по потолку начнёт выбрасывать настоящие вырезки ради мусора, то есть отчёт будет терять картинки тем чаще, чем дольше рецензент работает.
  Файлы: `src/entities/cutout/model/buffer.ts`, `src/app/ui/Root.tsx` либо `src/features/print-report/`.
  **Логирование:** INFO при очистке — сколько вырезок снято и по какой причине (после печати / удалена запись / нет записи). Идентификаторы записей уровнем DEBUG.

### Фаза 5. Тесты, документы и приёмка

- [x] **Задача 13: Тесты модели.** (зависит от 3, 4, 5, 8, 11)
  Тесты по нынешним приёмам проекта: Vitest, jsdom, рендеринг компонентов **самим Preact** без `@testing-library` (её в проекте нет и заводить не надо), поиск по доступному имени, а не по тестовым идентификаторам. `localStorage` уже подменён `MemoryStorage` из `vitest.setup.ts` — на него можно опираться, как это делает `storage.test.ts`.
  Что покрыть:
  — `src/entities/cutout/model/check.test.ts` — **самое важное**: контраст белого по белому даёт 1.00 и отбраковывается; белый на тёмно-синем и серый на белом проходят. Это единственная защита от кривой картинки в документе;
  — `src/entities/cutout/model/buffer.test.ts` — вытеснение по потолку; **отказ буфера не трогает `ENTRIES_KEY` и не выставляет `persistFailed` записей** — прямая проверка инварианта «жертвуем вырезку, а не запись»; `keep()` снимает вырезки, чьих записей в наборе нет, и не трогает остальные. Вытеснение по потолку идёт по ВОЗРАСТУ: отличить сироту от живой вырезки сам буфер не может — набор записей лежит в соседнем слайсе того же слоя, — и сирот снимает `keep()` перед печатью, когда набор устоялся;
  — `src/entities/cutout/model/build.test.ts` — вкомпилирование стилей, снятие `class`, вырезание `script`/`style`, **снятие атрибутов `on*` и ссылок со схемой `javascript:`** (документ уезжает в iframe того же origin), возврат `null` при превышении потолка;
  — `src/features/print-report/model/report.test.ts` — порядок разделов («Требует решения» впереди), **порядок страниц устойчив: сборка одного и того же набора из разных текущих маршрутов даёт одинаковый документ**, **запись `text-override` с непустым `style` несёт помеченную строку пожелания оформления**, сквозная нумерация совпадает с `numbering()`, запись без вырезки помечена явно, имя файла обмена присутствует, отсутствие имени даёт прямую фразу «файл ещё не выгружен» вместо выдуманного имени, метка `comment` считается от `anchorBox`, а не от всего кадра, **в разметке нет запрещённых техтерминов** (FR-36) — по образцу списка `FORBIDDEN` из `Root.test.tsx` и `ReviewMode.test.tsx`.
    **Изъятие для имени файла — назвать в тесте явно.** `FORBIDDEN` в `src/app/ui/Root.test.tsx:27` — это `['селектор','якорь','dom','json','формат','импорт']`, проверяемые как `not.toContain` по всему тексту в нижнем регистре. Имя файла обмена оканчивается на `.json`, то есть список, скопированный «по образцу» дословно, валит тест ровно на той строке, ради которой отчёт затевался (задача 8, смягчение RISK-002). Поэтому запрещённые слова ищутся в тексте документа **за вычетом узла с именем файла** (либо список для отчёта берётся без `json`), а присутствие имени проверяется отдельным ассертом. Изъятие ровно одно и касается только имени: «формат», «селектор», «якорь», «DOM» в отчёте по-прежнему запрещены целиком;
  — `src/features/export-entries/model/export.test.ts` — скачивается ровно один файл, набор помечается экспортированным при успехе.
  **Логирование:** в тестах логгер не проверяется, но и не глушится. Ассертов на текст логов не писать — они ломаются при первой же правке формулировки.

- [x] **Задача 14: Синхронизация документов.** (зависит от 11)
  Отчёт меняет контракт выгрузки, и это записано **в шести местах**, каждое из которых станет ложным:
  — `PRD — Калька.md`: FR-27 («два файла», строка 212) и сценарий А раздела 6, шаг 8 («скачиваются два файла: JSON и читаемая таблица», строка 125);
  — `.ai-factory/DESCRIPTION.md`, две строки: «Обмен идёт файлом: рецензент экспортирует JSON и читаемую таблицу» (13) и «**Экспорт двух файлов**: JSON для агента и импорта, читаемая таблица» (33);
  — `.claude/skills/kalka-exchange-format/SKILL.md`: **раздел «## Экспорт: два файла» ЦЕЛИКОМ** (заголовок 270, поясняющая строка 272 «FR-27 — JSON для агента и импорта, читаемая таблица для человека. Колонки таблицы:» и таблица колонок под ней), плюс жёсткий пункт чек-листа 329 «Экспорт даёт два файла, имя содержит дату и адрес»;
  — `.claude/skills/kalka-exchange-format/references/export-import.md`: **раздел «## Экспорт двух файлов» ЦЕЛИКОМ** (заголовок 5, строка 7 «FR-27 требует два файла» и таблица «Файл / Читатель / Роль» под ней), плюс пункт чек-листа 275 «Экспорт даёт **ровно** два файла»;
  — `README.md:99`: строка таблицы стендов «`dev/storage.html` | хранение в браузере и **выгрузка двух файлов**».
  **Править разделы навыка целиком, а не по номерам строк.** Исполнитель, поправивший только заголовок и пункт чек-листа, оставит под ними связный текст с таблицей, описывающей несуществующий второй файл, — и следующий читатель навыка поверит именно ему. Пункты чек-листов особенно важны: дошедший до них упрётся в требование, противоречащее сделанному, и остановится.
  Формат обмена при этом **не меняется ни в одном байте** — ни `format: 1`, ни состав `Entry`, ни шапка `about`. Меняется только второй файл выгрузки.
  Документы вести через `/aif-docs`; PRD и DESCRIPTION этому плану не принадлежат — правку согласовать.
  Файлы: `PRD — Калька.md`, `.ai-factory/DESCRIPTION.md`, `README.md`, `.claude/skills/kalka-exchange-format/SKILL.md`, `.claude/skills/kalka-exchange-format/references/export-import.md`.
  **Логирование:** задача документарная, кода не трогает — логировать нечего.

- [ ] **Задача 15: Стенд отчёта и ручной прогон печати.** (зависит от 9, 10, 12, 19, 20)
  Задача 13 — это Vitest в jsdom, а jsdom не печатает. Два риска из шести живут исключительно на стороне браузера, и оба сегодня остались бы написанными и непроверенными: RISK-004 (браузер по умолчанию не печатает фоны, и вырезка приедет белым листом — смягчение `print-color-adjust: exact` из задачи 8) и RISK-005 (Safari исторически требовал ненулевой видимости узла при печати из `<iframe>` внутри теневого корня — смягчение из задачи 7). Продукт вехи — напечатанный PDF (DEC-001), и не проверить его в браузере значит не проверить веху.
  Приём в проекте уже заведён: одиннадцать стендов в `dev/`, ручной прогон отдельной задачей и отдельным коммитом (`feature-ui-refactor.md`, задача 17), таблица стендов в README.
  — новый стенд `dev/report.html` с четырьмя случаями гейта RISK-001: абзац в светлой секции, карточка в сетке с картинкой, белый заголовок на тёмной секции и заголовок героя поверх фотографии (последний обязан отбраковаться самопроверкой и деградировать в текст — это приёмочный случай DEC-007 и DEC-013);
  — строка стенда добавляется в таблицу README (там же, где правится строка 99 задачей 14);
  — ручной прогон в Chrome, Safari и Firefox с параметром `?kalka`. Проверяется: печать вообще стартует из теневого корня; фоны вырезок печатаются, а не приезжают белым листом; шрифты носителя доезжают и переносы строк не уехали (DEC-016); метки области и указателя стоят на своих местах на бумаге, а не смещены (задача 9); сквозная нумерация на бумаге совпадает с номерами на экране; имя файла обмена в рамке названо верно.
  Результат прогона записать в задаче словами — какой браузер, что вышло. Провал любого пункта — работа, а не примечание.
  Файлы: `dev/report.html`, `README.md`.

  **Сделано 2026-09-06 (стенд и часть прогона в Chrome):**
  Стенд `dev/report.html` заведён, строка добавлена в таблицу стендов README.
  В Chrome на стенде проверено автоматизацией — то есть всё, что можно проверить,
  не открывая нативный диалог печати:
  — съёмка вырезки работает на настоящем `getComputedStyle`: 1660 байт разметки
    на абзац (замер гейта давал 1.2–11.7 КБ на элемент, порядок тот же),
    стили вкомпилированы, `class` снят, обработчиков событий не осталось;
  — `anchorBox` у узкого кадра равен всему кадру, как и задумано;
  — **гейт RISK-001 воспроизвёлся точно**: абзац в светлой секции прошёл
    самопроверку, белый заголовок на тёмной секции прошёл (приём «фон ближайшего
    непрозрачного предка» работает), заголовок героя поверх абсолютных слоёв
    ОТБРАКОВАН по контрасту и в буфер не попал — в логе
    `[kalka:cutout:check] вырезка отбракована по контрасту`;
  — кнопка «Отчёт» есть на месте, правка сохраняется вместе со своей вырезкой;
  — вырезки-сироты от неоконченных черновиков действительно копятся (после трёх
    разведочных кликов и одной сохранённой правки в буфере лежало 4 вырезки
    на 1 запись) — то есть сбор сирот задачи 12 не теоретический.

  **Прогон нашёл ДВА дефекта, оба починены — ради этого задача и заведена.**

  1. **Вырезка не уносила фон с собой.** Абзац из тёмной секции приехал в документ
     бледно-голубым по белой бумаге, то есть нечитаемым, и прошёл самопроверку
     насквозь. Дефект был не в проверке: она мерила фон ближайшего непрозрачного
     предка НА СТРАНИЦЕ и мерила верно — а в кадр этот фон не переносился,
     потому что красит абзац секция-предок, в кадр не попавшая. DEC-007 требует
     мерить «фон, который вырезка получит В ОТЧЁТЕ», и до починки эти два фона
     расходились. Теперь сборка вкомпилировывает фон в кадр (`paintBackground`
     в `build.ts`), а обе стороны спрашивают его у одной функции
     (`entities/cutout/model/color.ts`) — иначе они однажды разойдутся снова.
  2. **Очистка после печати сносила вырезку следующей правки.** Очистка
     срабатывает на первом изменении набора, а первое изменение — это сохранение
     СЛЕДУЮЩЕЙ правки, чья вырезка к тому моменту уже снята. `clear()` убивал
     её вместе с устаревшими: после каждой печати следующая правка молча выходила
     бы в отчёт без картинки. Теперь список снимаемого фиксируется в момент
     взведения — устаревают ровно те вырезки, что попали в напечатанный документ.

  Оба дефекта закрыты тестами (`build.test.ts`, `cleanup.test.ts`): в jsdom они
  не проявлялись, но воспроизводятся в нём, когда знаешь, что искать.

  **Прогон 2026-09-06, второй заход: печать в Chrome ПОЛУЧЕНА, и она нашла ещё два дефекта.**
  Прежняя запись «автоматизацией недостижимо» верна только для нативного диалога.
  Безголовый Chrome печатает документ в PDF без всякого диалога
  (`--headless --print-to-pdf`), и это НАСТОЯЩИЙ печатный вывод: `@page`, разрывы
  страниц, `print-color-adjust`, шрифты — всё считается по-печатному. Документ
  снят перехватом со стенда на четырёх случаях гейта RISK-001 и напечатан.

  **Дефект 3. Поля корня кадра ломали ВСЕ картинки документа — все три из трёх.**
  `getComputedStyle` отдаёт ИСПОЛЬЗОВАННОЕ значение полей, а не объявленное:
  у блока с `margin: 0 auto` это настоящие пиксели центрирования (на ширине 1440
  вышло 274), у заголовка — его верхнее поле. Они вкомпилировывались в корень
  клона и сдвигали содержимое ВНУТРИ кадра, размер которого снят
  с `getBoundingClientRect` — а он полей не включает. На бумаге вышло: карточка
  срезана посередине (содержимое уехало вправо на 226px), а тёмный заголовок
  и абзац — пустые полоски высотой 23 и 19 пикселей вместо строк текста.
  Починка: `dropOuterMargins` снимает поля у КОРНЯ кадра сразу после переноса
  стилей; поля детей остаются — на них держится расстояние между строками
  и они уже вошли в высоту кадра. В jsdom дефект не воспроизводится в принципе:
  там нет раскладки, и `auto` остаётся `auto`, — тест поэтому задаёт поля числом.

  **Дефект 4. Вшитые картинки выбрасывались вместе со всеми `data:`.**
  Белый список схем не различал `data:image/` у `<img>` и `data:text/html`
  где угодно, и обе картинки карточки приехали битыми значками. Прототипы держат
  значки и логотипы вшитыми, так что это не частность стенда. Починка узкая
  дважды: только тег `IMG` (у `<iframe>`, `<object>`, `<embed>` тот же атрибут
  `src`, и вот там `data:text/html` исполняется) и только тип `image/` — картинка
  в `<img>` живёт в защищённом статическом режиме без скриптов, даже когда
  внутри SVG. Ссылкам исключение не даётся вовсе.

  Оба дефекта закрыты тестами в `build.test.ts`.

  **Что подтверждено НА БУМАГЕ после починок (Chrome, A4):**
  — RISK-004 снят: фоны печатаются. Тёмная секция приехала тёмно-синей с белым
    заголовком, карточки белыми на светло-сером, пилюли цветными — то есть
    `print-color-adjust: exact` работает и приём «фон ближайшего непрозрачного
    предка» верен не только на экране;
  — DEC-016 снят: строка вырезки набрана объявленным семейством стенда, а не
    засечным Georgia остального документа. Правило `@font-face` доехало;
  — карточка осталась СЕТКОЙ: две колонки, картинки, пилюли, ссылки — не
    рассыпалась в вертикальный список;
  — метки стоят на своих местах: рамка облегает обведённую карточку, номер метки
    на кадре совпадает с номером записи;
  — сквозная нумерация 1–4 сквозь оба раздела, порядок разделов «Требует
    решения» → «Правки текста», в рамке назван файл `kalka-…-0239.json`;
  — заголовок героя поверх абсолютных слоёв отбракован по контрасту и показан
    текстом с пометкой — приёмочный случай DEC-007 и DEC-013 на бумаге.

  **Дефект 5 — НЕ починен, решение за владельцем плана.** Кадр правки текста
  показывает ОДНУ строку вместо трёх: высота снята по живому элементу, а он
  несёт короткое «стало», тогда как подставленное «было» втрое длиннее.
  Это ровно та оговорка, что записана в шапке `build.ts` и в пункте 3 «Уточнений
  плана», — но на бумаге она весит больше, чем обещало слово «обрезан»: пропала
  не кромка, а две строки из трёх. Довод «померить клон нельзя, он отсоединён
  от документа» при этом неверен: клон можно приложить за экраном и померить —
  например, в теневом корне или в самом кадре отчёта. Это переделка съёмки,
  а не правка задачи 15, поэтому вынесена сюда, а не сделана молча.

  **Остаётся человеку — и это меньше, чем было.** Безголовая печать обходит
  сам путь печати: документ печатается напрямую, а не из `<iframe>` внутри
  теневого корня. Значит RISK-005 (Safari исторически требовал ненулевой
  видимости узла) не проверен вовсе, и Firefox с Safari не проверены ни в одном
  пункте. Человеку остаётся открыть `dev/report.html?kalka` в Safari и Firefox,
  нажать «Отчёт» и убедиться, что печать вообще стартует и лист не пуст;
  остальные шесть пунктов списка в Chrome уже сняты.

  **Остаток задачи снимается ПОСЛЕ фазы 6 и задачи 20, а не до них.** Прогон
  ручной и в трёх браузерах, то есть переснять его дёшево не выйдет, а фаза 6
  меняет то, что доезжает до бумаги, по существу: сегодня буфер на старом наборе
  пуст и картинок в документе почти нет, а после задачи 18 картинка появляется
  у каждой записи, чьё место есть на текущей странице. Прогон, снятый до фазы 6,
  проверял бы конвейер, которого после неё не останется. Задача 20 нужна раньше
  прогона по другой причине: без своего шрифта на стенде пункт «шрифты носителя
  доезжают» проверить физически нечем.

  **Осталось человеку** (автоматизацией недостижимо — печать открывает нативный
  диалог, блокирующий вкладку): сам напечатанный документ в Chrome, Safari
  и Firefox по семи пунктам списка «Что смотреть» на стенде. Известно, что печать
  СТАРТУЕТ (диалог Chrome открылся) и что собранный документ верен — он снят
  перехватом и разобран: порядок разделов, сквозная нумерация из хранилища,
  метка на кадре, рамка про невыгруженный файл, отсутствие запрещённых терминов.
  НЕ проверено: печатаются ли фоны на бумаге, доезжают ли правила `@font-face`
  (на стенде своих шрифтов нет — путь DEC-016 не задействован вовсе), стоят ли
  метки на бумаге и печатает ли Safari из узла в теневом корне.
  **Логирование:** задача проверочная, кода виджета не трогает — логировать нечего.

- [x] **Задача 16: Замер прироста бандла.** (зависит от 11, 15)
  План назначает потолок «на отчёт целиком 15 КБ gzip, не влезло — урезается отчёт» и выносит его в Success signals, но проверить его нечем: `scripts/check-bundle.mjs:15` знает только общий предел `LIMIT = 100 * 1024` и упадёт лишь при выходе за 100 КБ. Потолок в 15 КБ существует на словах, а замер обязан быть числом.
  Отправная точка записана в разделе Constraints: замер 2026-09-05 на `dist/kalka.js` — 33 695 байт gzip, то есть занято 32.9 КБ в единицах `check-bundle.mjs`, свободно 67.1.
  — `npm run build`, взять gzip-размер `dist/kalka.js`, вычесть отправную точку, получить прирост;
  — замер снимается ПОСЛЕ задачи 11: удаление `toTableText`, `rowOf`, `cell` и `formatMoment` уменьшает бандл и входит в итог. Замер до неё дал бы завышенный прирост и спровоцировал ненужную резку отчёта;
  — записать число туда, где бюджет уже называется: `README.md` и `.ai-factory/DESCRIPTION.md`. Методику замера записать рядом с числом — расхождение прежнего замера («29 КБ») с нынешним осталось необъяснённым именно потому, что методику не записали (Open questions);
  — выход за 15 КБ — повод резать отчёт, а не поднимать потолок. Что резать первым: правила `@font-face` (DEC-016) дороже всего и деградируют мягче всего.
  Файлы: `README.md`, `.ai-factory/DESCRIPTION.md`.
  **Логирование:** задача измерительная, кода не трогает — логировать нечего.

### Фаза 6. Второй источник вырезки: съёмка в момент отчёта

Обоснование пересмотра целиком — в разделе «Уточнения плана к исследованию», пункт 3.
Коротко: съёмка в момент правки остаётся и переживает переход между страницами,
а рядом появляется съёмка в момент отчёта для записей, чьё место найдено
на текущей странице. Без неё правки, сделанные до появления вырезок или пережившие
очистку после печати, остаются без картинок навсегда.

- [x] **Задача 17: Синхронная съёмка вырезки по требованию.** (зависит от 3, 4, 6)
  Сейчас `captureCutout` — единственный вход в съёмку: отложен на `CUTOUT_CAPTURE_IDLE_MS`,
  наружу ничего не отдаёт и пишет прямо в буфер. Отчёту нужен ТОТ ЖЕ порядок
  («проверить контраст → расширить кадр у замечаний → собрать»), но синхронно
  и с результатом на руки. Правило расширения кадра заперто в приватной `widen`
  (`src/entities/cutout/model/capture.ts:60`), и написать его второй раз значило бы
  завести две копии, которые однажды разойдутся — а разойдясь, дадут на бумаге
  кадр не того размера, чем на экране.
  — вынести из `capture.ts` синхронную `makeCutout({ anchor, wide, replaceContent }): Cutout | null`
    и вывести её в публичный API слайса; `captureCutout` оставить отложенной обёрткой
    над ней, снаружи её поведение не меняется;
  — `src/entities/cutout/model/build.ts`: `buildCutout` принимает необязательный
    `replaceContent?: Node`. Подмена применяется ПОСЛЕ переноса стилей и абсолютизации
    картинок, но ДО чистки: перенос стилей обходит живое дерево и клон парами,
    и подменённое поддерево сломало бы соответствие; чистка же обязана пройти
    и по подставленному содержимому тоже;
  — записать в шапке `build.ts` обе оговорки пункта 3 «Уточнений»: потерю
    вкомпилированных стилей у вложенных узлов и то, что размер кадра измерен
    по живому элементу с текстом «стало»;
  — поправить описание `capture.ts` в `.ai-factory/ARCHITECTURE.md`: «порядок
    «собрать → проверить → положить»» после этой задачи описывает только обёртку.
  Файлы: `src/entities/cutout/model/capture.ts`, `src/entities/cutout/model/build.ts`,
  `src/entities/cutout/index.ts`, `.ai-factory/ARCHITECTURE.md`.
  **Логирование:** `makeCutout` наследует нынешние области — `cutout:check`,
  `cutout:build`. Своего сообщения не заводить: она не новое действие, а тот же
  порядок без отсрочки и без записи в буфер. У `captureCutout` логи остаются как есть.

- [x] **Задача 18: Отчёт снимает недостающие вырезки на месте.** (зависит от 8, 17)
  Новый `src/features/print-report/model/collect.ts` с `cutoutFor(entry, doc): Cutout | null`:
  — сперва буфер (`cutoutBuffer.get`) — у него приоритет: снятое в момент правки
    заведомо показывает исходное состояние, догадок не требует и уже проверено;
  — если в буфере пусто — живой элемент из `entryStore.statusOf(entry.id)?.element`
    с **обязательной** проверкой `isConnected`: шапка `EntryState.element`
    (`src/entities/entry/model/store.ts`) прямо этого требует — носитель мог
    перерисовать блок между проходом наложения и печатью;
  — элемента нет или он оторван — возвращаем `null`. Это запись с ДРУГОЙ страницы,
    и снимать её нечем: в текущем DOM её места не существует. Отчёт покажет текст
    с пометкой, как и раньше;
  — `wide: entry.type === 'comment'` — та же ширина кадра, что при съёмке в момент
    правки: у замечаний с окрестностью, у правок текста по элементу;
  — для `text-override` подмена содержимого на `sanitizeHtml(entry.wasHtml, doc)`:
    элемент несёт `now`, а на картинке должно быть «было». Подмена идёт
    в ОТСОЕДИНЁННОМ клоне — страница носителя не трогается вовсе;
  — снятое на месте в буфер **не пишется**: источник доступен, пока страница открыта,
    а буфер существует ради записей с других страниц. Писать туда значило бы
    раздувать его тем, что и так под рукой.
  Дальше `src/app/ui/Root.tsx` отдаёт вырезки в `buildReport` — но **картой, снятой
  один раз, а не функцией `cutoutFor` напрямую**.
  **Почему не напрямую.** `input.cutoutOf` зовётся не один раз на запись, а три:
  в блоке записи (`report.ts:175`), в счётчике `withShot` (`report.ts:382`) и при
  сборе `fontFaces` (`report.ts:396`). Пока за ним стоит `cutoutBuffer.get`, это три
  чтения из памяти; подставь туда съёмку на месте — и каждая запись клонируется
  и обходится `getComputedStyle` трижды, у замечаний ещё и по расширенному кадру,
  всё синхронно на главном такте прямо перед открытием диалога печати. Это ровно то,
  что запрещает DEC-009, только в худшем месте, и вдобавок три вызова дадут три
  разных `at` у одной картинки. Сигнатура тоже не сходится буквально:
  `ReportInput.cutoutOf` объявлен как `(entryId: string) => Cutout | null`
  (`report.ts:49`), а `cutoutFor` нужна запись целиком — ради `entry.type`
  и `entry.wasHtml`.
  Поэтому в `Root.tsx`: `const shots = new Map(entries.map((e) => [e.id, cutoutFor(e, document)]))`,
  и дальше `cutoutOf: (id) => shots.get(id) ?? null`. `ReportInput` не меняется,
  съёмка на запись ровно одна.
  Отдельно поправить текст `noShots()` в `model/report.ts`: фраза «снимок делается
  в момент правки» после этой задачи станет ложной — картинка появится сама,
  если место найдено на текущей странице. Новая формулировка должна говорить, что
  картинок нет у записей с других страниц и у мест, которые снять не удалось.
  Ещё поправить `.ai-factory/ARCHITECTURE.md`: строки 106–107 перечисляют обязанности
  `print-report/` поимённо — «сборка документа, метка поверх вырезки, срок жизни
  буфера», — а `collect.ts` добавляет четвёртую: выбор источника вырезки (сперва
  буфер, затем съёмка по живому элементу). Тот же приём, что в задаче 17 с описанием
  `capture.ts`; описание слайса, ради которого веха и затеяна, не имеет права
  остаться неполным.
  Файлы: `src/features/print-report/model/collect.ts`, `src/features/print-report/index.ts`,
  `src/features/print-report/model/report.ts`, `src/app/ui/Root.tsx`,
  `.ai-factory/ARCHITECTURE.md`.
  **Логирование:** `createLogger('report:collect')`. DEBUG на каждую запись —
  идентификатор, тип, откуда взята вырезка (`буфер` / `снята на месте` / `нет`);
  INFO итогом на сборку — сколько из буфера, сколько снято на месте, сколько без
  картинки. Ни текстов правок, ни адреса сайта, ни разметки вырезки в лог не уходит.

- [x] **Задача 19: Тесты второго источника.** (зависит от 17, 18)
  — `src/entities/cutout/model/build.test.ts`: `replaceContent` подставляет
    переданное содержимое в клон; **живой элемент при этом не меняется** — прямая
    проверка того, что страница носителя не трогается; чистка проходит
    и по подставленному содержимому (обработчик событий в нём тоже снимается);
  — `src/features/print-report/model/collect.test.ts`: буфер имеет приоритет над
    съёмкой на месте; при пустом буфере вырезка снимается по элементу из хранилища;
    запись без элемента и запись с `isConnected: false` дают `null`, а не бросок;
    у `text-override` в разметку попадает текст из `wasHtml`, а не из `now`;
    у `comment` кадр расширяется, у правки текста — нет;
  — **там же два случая про новую дверь в документ, и первый из них — главный
    в задаче.** До фазы 6 дверь была одна: буфер, куда кладёт только `captureCutout`
    и только после `checkCutout`. Задача 18 открывает вторую, минуя буфер, и обещание
    Success signals «ни одна кривая вырезка не попала в напечатанный документ»
    держится теперь на двух замках вместо одного. Проверить надо оба:
    элемент с провальным контрастом (белым по белому, случай уже собран
    в `check.test.ts`) даёт из `cutoutFor` `null`, а не картинку; и снятое на месте
    в буфер **не пишется** — `cutoutBuffer.has(entry.id)` после успешной съёмки
    остаётся `false`. Второе объявлено инвариантом в задаче 18, а без теста
    инвариантом не является: запись туда раздувала бы буфер тем, что и так под рукой.
  Файлы: `src/entities/cutout/model/build.test.ts`, `src/features/print-report/model/collect.test.ts`.
  **Логирование:** в тестах логгер не проверяется и не глушится; ассертов на текст
  логов не писать.
  **После этой задачи переснять замер бандла.** Задача 16 выполнена и её число
  записано в `README.md` и `.ai-factory/DESCRIPTION.md`; фаза 6 добавляет код,
  и записанное число разъедется с фактом. Заново отмечать задачу 16 не нужно —
  достаточно повторить её замер той же методикой и обновить оба места.

- [x] **Задача 20: Собственный шрифт на стенде — иначе DEC-016 не проверяется ничем.** (зависит от 8)
  Задача 15 признаёт это прямо: «на стенде своих шрифтов нет — путь DEC-016
  не задействован вовсе». Признание есть, вывода нет, и в таком виде решение
  уезжает в веху непроверенным. За DEC-016 стоит замер (строка «55–200 т» заняла
  58.6px на сайте и 60.66px в вырезке — трёх процентов хватает, чтобы сместить
  перенос), отдельный код на трёх участках (`collectFontFaces` в
  `src/entities/cutout/model/build.ts`, сбор правил со всех вырезок
  в `report.ts`, объявление их в `styles()`), обход `SecurityError` на таблицах
  с чужого origin — и пункт «шрифты носителя доезжают и переносы строк не уехали»
  в списке «Что смотреть» задачи 15. Проверено: в `dev/report.html` нет ни одного
  `@font-face`. Значит человек, дошедший до этого пункта прогона, проверить его
  физически не может, а первый настоящий прогон пути случится уже на прототипе
  заказчика.
  — в `dev/report.html` объявить `@font-face` со шрифтом, лежащим рядом со стендом
    (локальный файл в `dev/` либо `data:`-адрес прямо в объявлении) — и применить
    его к абзацу и заголовку хотя бы одной из четырёх секций стенда. Взять шрифт
    с метриками, ЗАМЕТНО отличными от системного: смысл проверки в том, чтобы
    подмена была видна глазом, а не выводилась из чисел;
  — запрет внешних адресов из `scripts/check-bundle.mjs` относится к БАНДЛУ,
    а не к стендам в `dev/` (RISK-006, там же разобрана эта путаница). Но адрес
    всё равно брать местный, а не с CDN: стенд обязан работать без сети, как
    остальные одиннадцать;
  — в списке «Что смотреть» уточнить пункт про шрифты: какая именно строка на какой
    секции сравнивается и по какому признаку видно подмену (перенос строки,
    ширина строки, начертание).
  Файлы: `dev/report.html`.
  **Логирование:** задача стендовая, кода виджета не трогает — логировать нечего.

  **Сделано 2026-09-06. Взят `local()`, а не файл шрифта — и вот почему.**
  Стенды обязаны работать без сети (как остальные одиннадцать), чужой шрифт нельзя
  ни положить в репозиторий по лицензии, ни тянуть с CDN, а `fontTools` для сборки
  своего в системе нет. `local()` даёт настоящее правило `@font-face`: движок читает
  его как `CSSFontFaceRule` и копирует в отчёт, а семейство `KalkaStandFace` известно
  ТОЛЬКО по этому правилу — не доехало оно, и текст откатывается на Georgia.
  Объявлено в шапке стенда, применено к заголовку и абзацу секции «Чем занимаемся»
  (случай 1: простая строка в узком кадре — та самая мера, на которой замерен DEC-016).

  **Проверено в Chrome на живом стенде (автоматизацией, без нативного диалога):**
  — со страницы собирается ровно 1 правило `@font-face`, до этой задачи собиралось 0;
  — расхождение метрик 11.1% (571.11px против 513.97px на одной строке) — DEC-016
    замерил, что хватает и 3%, так что подмена видна глазом, а не выводится из чисел;
  — **сквозная проверка при ПУСТОМ буфере:** запись наложена, вырезка снята на месте
    (задача 18), правило `@font-face` дословно оказалось в напечатанном документе,
    `font-family` кадра — `KalkaStandFace, Georgia, serif`;
  — в кадре стоит «было» («Проектирование, изготовление…»), а не «стало»
    («Короткая замена.») — подмена `wasHtml` работает на живом браузере;
  — после печати `kalka:cutouts` остался пуст: снятое на месте в буфер не пишется.

  **Чего этот случай НЕ покрывает и покрыть не может:** загрузку файла шрифта
  по адресу с сервера прототипа. Это проверяется только на живом прототипе
  со своими шрифтами; в списке «Что смотреть» оговорка записана прямо.

- [x] **Задача 21: Кадр становится полосой во всю ширину страницы.** (зависит от 8, 9, 18)
  Обоснование пересмотра целиком — в разделе «Уточнения плана к исследованию», пункт 4.
  — `Cutout` получает поле `band` — видимую полосу внутри кадра (отступ сверху
    и высота, в px кадра). Отдельно от `width`/`height`, а не вместо них: разметка
    клона раскладывается по полной высоте кадра, и подмени мы высоту — вёрстка
    внутри поехала бы. Обрезка это дело отчёта, а не съёмки;
  — `widen` заменяется на `pageWide`: подъём до предка во всю ширину (порог
    `CUTOUT_FRAME_FULL_WIDTH_RATIO`, глубина поднята с 3 до 8 — секция кадром больше
    не страшна, по вертикали её всё равно режет полоса). `<body>` кадром по-прежнему
    не берётся: он тащит шапку сайта и подвал;
  — `widenToPage` в `build.ts` оборачивает клон холстом шириной со страницу и ставит
    кадр на его место полем холста. Полем ХОЛСТА, а не клона: у клона поля только что
    сняты (`dropOuterMargins`), и вернуть одно из них значило бы спорить с этим
    решением через сокращённую запись `margin`;
  — `anchorBox.x` считается теперь от левого края СТРАНИЦЫ, а не кадра: холст и есть
    вырезка, метка ставится в его координатах. По вертикали — по-прежнему от верха кадра;
  — `wide` меняет смысл: не ширину кадра, а высоту полосы. У замечаний запас вдвое
    больше (`CUTOUT_BAND_PAD_WIDE_PX`), потому что «это убрать» без окружающего блока
    не значит ничего;
  — отчёт обрезает по полосе: коробка ростом с `band.h`, кадр сдвинут
    `scale(s) translateY(-band.y)`. Порядок преобразований именно такой — сдвиг задаётся
    в пикселях НЕСЖАТОГО кадра, тех же, в которых посчитан `anchorBox` метки,
    и обе величины остаются в одной системе координат.
  Файлы: `src/shared/model/layer.ts`, `src/shared/config/constants.ts`,
  `src/entities/cutout/model/build.ts`, `src/entities/cutout/model/capture.ts`,
  `src/features/print-report/model/report.ts`, `dev/report.html`, тесты.
  **Логирование:** новых сообщений не заводится — полоса это геометрия кадра,
  а её уже описывает DEBUG «вырезка собрана».

  **Проверено печатью 2026-09-06.** Три вырезки документа вышли одной ширины
  и одного масштаба (0.431 против прежних 0.731/0.431 вразнобой). Тёмная секция
  печатается полосой во всю ширину с заголовком и абзацем под ним — место узнаётся.
  Запас полосы поднят с 32 до 80 пикселей после первого прогона: 32 пикселя страницы
  превращаются на бумаге в 14, то есть в нитку. При 80 полоса даёт на бумаге около
  35 пикселей — те самые две строки, ради которых запас и заведён.

## Что этот план сознательно не делает

- **Не чинит случай «фон рисуют соседние слои».** Заголовок героя поверх фотографии остаётся неснимаемым: приём «фон ближайшего непрозрачного предка» даёт белый, и вырезка выходит белым по белому. План только **обнаруживает** это (задача 4) и честно деградирует в текст (задача 8). Починка не проверялась ни разу; разведка показала, что материал для фона под заголовком есть, но собрать из него фон не пробовали.
- **Не переносит `::before`/`::after`.** На гейте не проявилось, но и не проверялось прицельно.
- **Не делает отчёт круга проверки.** Тот же рендерер, но после вехи «Импорт, разбор и режим проверки»: троек исходов `ok`/`bad`/`unclear` пока не из чего собирать. Сегодня закладывается только необязательное поле исхода в модели данных отчёта — без «конфигурации режимов».
- **Не снимает вырезки для записей с других страниц.** Съёмка в момент отчёта (задача 18)
  работает только там, где место найдено в текущем DOM. Для чужих страниц остаётся буфер,
  наполняемый в момент правки; не заходил рецензент на страницу — картинки не будет.
  Обходить это, открывая страницы в скрытых кадрах, план не берётся: виджет — гость,
  и ходить по чужому сайту сам он не имеет права.
