/*
 * Публичный API слайса «правка текста». Без `export *` — барели расходуют
 * бюджет бандла (NFR-01), и наружу выходит только действительно нужное.
 *
 * `model/*` наружу не выходит: захват записи, поиск существующей правки
 * и сохранение — внутренняя механика слайса, и меняться она вправе свободно,
 * пока публичный API стабилен.
 */
export { useTextTool } from './model/tool'
export type { TextTool } from './model/tool'
