/*
 * Публичный API слайса «область». Без `export *` — барели расходуют бюджет
 * бандла (NFR-01), и наружу выходит только действительно нужное.
 *
 * `model/*` наружу не выходит: сборка черновика и работа с режимом рисования —
 * внутренняя механика слайса, и меняться она вправе свободно, пока публичный
 * API стабилен.
 */
export { useDrawArea } from './model/tool'
export type { DrawAreaTool } from './model/tool'
