/*
 * Публичный API слайса «окно редактора текста». Без `export *` — барели
 * расходуют бюджет бандла (NFR-01).
 *
 * Наружу выходит только само окно: панель форматирования, редактируемая
 * область и выбор оформления — его внутренние части.
 */
export { TextEditor } from './ui/TextEditor'
export type { TextEditorProps } from './ui/TextEditor'
