/*
 * Публичный API слайса «якорь» — единственная точка входа снаружи.
 *
 * `export *` не используется: барель обязан экспортировать только то, что
 * действительно нужно снаружи, иначе бандлер втягивает весь слайс ради одного
 * экспорта, а бюджет 100 КБ gzip (NFR-01) этого не прощает.
 *
 * Внутренние вспомогательные функции (`buildSelector`, `trySnippet`, обход
 * текстовых узлов) наружу не выходят: структура слайса свободно меняется,
 * пока публичный API стабилен.
 */
export { captureAnchor } from './model/build'
export { resolveAnchor } from './model/resolve'
export { matchesWas, applyStatus } from './model/drift'
// Второй вход обхода текстовых узлов: поиск текста МИМО наложенного слоя.
// Нужен режиму проверки после передеплоя (FR-35) — см. шапку `model/walk.ts`.
export { hasTextOnPage } from './model/walk'
