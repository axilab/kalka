/*
 * Публичный API слайса «контекст для агента» — единственная точка входа снаружи.
 *
 * `export *` не используется: барель обязан экспортировать только то, что
 * действительно нужно снаружи, иначе бандлер втягивает весь слайс ради одного
 * экспорта, а бюджет 100 КБ gzip (NFR-01) этого не прощает.
 *
 * Наружу выходит ровно одна функция. `buildPageText`, `describeForHuman`,
 * `buildPath` и остальное остаются внутренними: структура слайса свободно
 * меняется, пока публичный API стабилен. Отдельно взятое поле снаружи никому
 * не нужно — нужен весь набор, собранный ОДНИМ проходом (решение 1), и любой
 * дополнительный экспорт был бы приглашением посчитать что-то вторым.
 */
export { captureAgentContext } from './model/capture'
